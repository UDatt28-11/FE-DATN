import React from 'react';
import './BookRoomButton.css';

const BookRoomButton: React.FC = () => {
  return (
    <a href="#" className="book-room-btn btn palatin-btn">
      Book Room
    </a>
  );
};

export default BookRoomButton;
